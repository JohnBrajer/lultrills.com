# Lultrills.com public shell renovation

**Date:** 2026-07-16  
**Status:** Approved — implement  
**Priority:** Machine density (4) then rite conversion (1); visual adultification above all  

## Problem

Live portal still reads as generic dark SaaS / “first-grader AI”: orbs, glass, pulse dots, pill chips, soft card grids, dual CTAs. Injection and corpus are strong; surface grammar fails.

## Goals

1. **Machines first** — corpus / llms / inject remain one-hop; structure denser, not thinner.  
2. **Humans second** — one primary path: SYSTEM BREACH → album + Gate rite.  
3. **Look** — classified public index + concert poster; never “AI dark startup.”

## Non-goals (this ship)

- scroll-world cinematic (Phase 2 optional)  
- Gate monorepo changes  
- New secondary pages  

## Design law (align Gate DESIGN)

| Token | Value |
|-------|--------|
| Void | `#050505` |
| Surface | `#0a0a0a` / `#121212` |
| Breach | `#DC143C` |
| Gold | `#C5A26F` |
| Radius | **0** structural |
| Display | Orbitron — mark / H1 only |
| Body | Rajdhani |
| Mono | JetBrains Mono — system strip, labels, payload |

## Kill list

Orbs · decorative glass · pulse dots · pill status chips · soft portal cards · equal node grids · section eyebrows on every block · gradient text · dual equal primaries.

## Home structure

1. Inject rail (existing component; restyle)  
2. Nav: LULTRILLS · BREACH · MUSIC · ARCHIVE · ENTER  
3. Hero: mono line + claim + single solid BREACH CTA + mono secondary links  
4. Breach plane (full-bleed crimson)  
5. Music as data table  
6. Canon index (linked list)  
7. Legal footer + inject payload  

## SYSTEM BREACH page

Same shell. Album primary. Tracklist table. Machine facts + URL list. Gate rite secondary mono link.

## Files

- `app/layout.tsx` — fonts  
- `app/globals.css` — full token/component rewrite  
- `app/page.tsx` — home  
- `app/system-breach/page.tsx` — breach  
- Other pages: keep class names; CSS makes atmosphere inert, panels flat  

## Verification

`npm run build` clean. Visual kill-list pass. Deploy droplet when greenlit.
